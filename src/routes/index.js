import { createBrowserRouter, Router } from "react-router-dom";
import { routes } from "./routePaths";
import { useFinalRoutes } from "./router";
export const router = <Router><getFinalRoutes/></Router>;